# express-query-adapter

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build express-query-adapter` to build the library.

## Running unit tests

Run `nx test express-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
